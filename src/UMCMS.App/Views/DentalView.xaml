<UserControl x:Class="UMCMS.App.Views.DentalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:v="clr-namespace:UMCMS.App.Views"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <Grid>
        <DockPanel>
            <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
                <DockPanel>
                    <Button x:Name="BtnNew" DockPanel.Dock="Right" Content="＋ New Dental Record" Style="{StaticResource PrimaryButton}" Click="BtnNew_Click" IsEnabled="False"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Patient:" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="FPatient" Height="38" Width="280" DisplayMemberPath="FullName" SelectionChanged="FPatient_SelectionChanged"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Style="{StaticResource Card}" Padding="0">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Dental record history" Style="{StaticResource H2}" Margin="14,12,0,8"/>
                        <DataGrid x:Name="Grid" AutoGenerateColumns="True" AutoGeneratingColumn="HideId"/>
                    </DockPanel>
                </Border>
                <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Previous prescriptions" Style="{StaticResource H2}" Margin="14,12,0,8"/>
                        <DataGrid x:Name="RxHistoryGrid" AutoGenerateColumns="True"/>
                    </DockPanel>
                </Border>
            </Grid>
        </DockPanel>

        <Border x:Name="Overlay" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="580" MaxHeight="640" VerticalAlignment="Center" HorizontalAlignment="Center">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="New Dental Record" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,14"/>
                        <TextBlock Text="Tooth chart — click a tooth to cycle its status" Style="{StaticResource SubtleText}"/>
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,4,0,6" Background="#FCFAFA">
                            <StackPanel>
                                <TextBlock Text="Upper" FontSize="10" Foreground="#999"/>
                                <WrapPanel x:Name="UpperPanel" Margin="0,2,0,8"/>
                                <TextBlock Text="Lower" FontSize="10" Foreground="#999"/>
                                <WrapPanel x:Name="LowerPanel" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <WrapPanel x:Name="LegendPanel" Margin="0,0,0,10"/>
                        <TextBlock Text="Gum health" Style="{StaticResource SubtleText}"/>
                        <ComboBox x:Name="DGum" Height="38" Margin="0,2,0,10">
                            <ComboBoxItem Content="Healthy" IsSelected="True"/><ComboBoxItem Content="Mild gingivitis"/>
                            <ComboBoxItem Content="Moderate"/><ComboBoxItem Content="Severe / periodontitis"/>
                        </ComboBox>
                        <TextBlock Text="X-ray notes" Style="{StaticResource SubtleText}"/>
                        <TextBox x:Name="DXray" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                        <TextBlock Text="Notes / treatment" Style="{StaticResource SubtleText}"/>
                        <TextBox x:Name="DNotes" Style="{StaticResource FormInput}" Height="60" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top" Margin="0,2,0,10"/>

                        <TextBlock Text="Prescriptions (optional — sent to the pharmacy)" Style="{StaticResource H2}" FontSize="13" Margin="0,6,0,4"/>
                        <v:PrescriptionEntry x:Name="RxEntry"/>

                        <TextBlock x:Name="TxtError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,8,0,8"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                            <Button Content="Save &amp; Send to Pharmacy" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSavePharmacy_Click"/>
                            <Button Content="Save &amp; Dispense Here" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnSaveDispense_Click"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Border>
    </Grid>
</UserControl>
