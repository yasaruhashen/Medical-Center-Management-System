<UserControl x:Class="UMCMS.App.Views.ForecastView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Reorder Forecast" Style="{StaticResource H2}" VerticalAlignment="Center"/>
                <TextBlock Text="Exponential smoothing on monthly consumption · suggested quantities" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="14,0,0,0"/>
                <TextBlock Text="Stream:" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="24,0,8,0"/>
                <ComboBox x:Name="FStream" Height="38" Width="150" SelectionChanged="Filter_Changed">
                    <ComboBoxItem Content="All" IsSelected="True"/><ComboBoxItem Content="General"/><ComboBoxItem Content="Dental"/>
                </ComboBox>
                <Button Content="🛒 Export Purchase Order" Style="{StaticResource PrimaryButton}" Margin="24,0,0,0" Click="BtnPo_Click"/>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="*"/>
                    <DataGridTextColumn Header="Stream" Binding="{Binding Stream}" Width="100"/>
                    <DataGridTextColumn Header="On hand" Binding="{Binding OnHand}" Width="90"/>
                    <DataGridTextColumn Header="Reorder at" Binding="{Binding ReorderLevel}" Width="100"/>
                    <DataGridTextColumn Header="Projected demand" Binding="{Binding ProjectedDemand}" Width="140"/>
                    <DataGridTextColumn Header="Suggested reorder" Binding="{Binding SuggestedReorder}" Width="150"/>
                    <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
