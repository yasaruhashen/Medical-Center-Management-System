<UserControl x:Class="UMCMS.App.Views.AvailabilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="My Availability" Style="{StaticResource H2}"/>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Current status:" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Border x:Name="StatusChip" CornerRadius="6" Padding="12,5" Background="#E8F6EC">
                        <TextBlock x:Name="TxtStatus" Text="On-site" FontWeight="SemiBold" Foreground="#1B7A3D"/>
                    </Border>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                    <Button Content="🟢 On-site" Style="{StaticResource PrimaryButton}" Tag="OnSite" Click="SetStatus_Click"/>
                    <Button Content="🟡 Away" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Tag="Away" Click="SetStatus_Click"/>
                    <Button Content="🔴 In Consultation" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Tag="InConsultation" Click="SetStatus_Click"/>
                </StackPanel>
                <TextBlock x:Name="TxtInfo" Style="{StaticResource SubtleText}" Margin="0,14,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource Card}" Padding="0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Notifications sent to subscribers" Style="{StaticResource H2}" Margin="14,12,0,8"/>
                <DataGrid x:Name="NotifGrid" AutoGenerateColumns="True"/>
            </DockPanel>
        </Border>
    </DockPanel>
</UserControl>
