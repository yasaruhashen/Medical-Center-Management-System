<UserControl x:Class="UMCMS.App.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <Grid>
        <DockPanel>
            <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Content="＋ Add User" Style="{StaticResource PrimaryButton}" Click="BtnAdd_Click"/>
                        <Button x:Name="BtnEdit" Content="Edit" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnEdit_Click" IsEnabled="False"/>
                        <Button x:Name="BtnToggle" Content="Activate / Deactivate" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnToggle_Click" IsEnabled="False"/>
                        <Button x:Name="BtnReset" Content="Reset Password" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnReset_Click" IsEnabled="False"/>
                    </StackPanel>
                    <TextBlock Text="User Management" Style="{StaticResource H2}" VerticalAlignment="Center"/>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource Card}" Padding="0">
                <DataGrid x:Name="Grid" SelectionChanged="Grid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*"/>
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="130"/>
                        <DataGridTextColumn Header="Specialization" Binding="{Binding Specialization}" Width="130"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                        <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </DockPanel>

        <!-- Add/Edit overlay -->
        <Border x:Name="OverlayEdit" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="480" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock x:Name="TxtTitle" Text="Add User" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,14"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0"><TextBlock Text="First name *" Style="{StaticResource SubtleText}"/><TextBox x:Name="UFirst" Style="{StaticResource FormInput}" Margin="0,2,0,10"/></StackPanel>
                        <StackPanel Grid.Column="2"><TextBlock Text="Last name *" Style="{StaticResource SubtleText}"/><TextBox x:Name="ULast" Style="{StaticResource FormInput}" Margin="0,2,0,10"/></StackPanel>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0"><TextBlock Text="Username *" Style="{StaticResource SubtleText}"/><TextBox x:Name="UUsername" Style="{StaticResource FormInput}" Margin="0,2,0,10"/></StackPanel>
                        <StackPanel Grid.Column="2" x:Name="PanelPassword"><TextBlock Text="Password *" Style="{StaticResource SubtleText}"/><PasswordBox x:Name="UPassword" Height="38" Margin="0,2,0,10"/></StackPanel>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Role *" Style="{StaticResource SubtleText}"/>
                            <ComboBox x:Name="URole" Height="38" Margin="0,2,0,10" SelectionChanged="URole_Changed">
                                <ComboBoxItem Content="Admin"/><ComboBoxItem Content="Doctor"/><ComboBoxItem Content="Receptionist"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2" x:Name="PanelSpec">
                            <TextBlock Text="Specialization" Style="{StaticResource SubtleText}"/>
                            <ComboBox x:Name="USpec" Height="38" Margin="0,2,0,10">
                                <ComboBoxItem Content="General" IsSelected="True"/><ComboBoxItem Content="Dental"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0"><TextBlock Text="Phone" Style="{StaticResource SubtleText}"/><TextBox x:Name="UPhone" Style="{StaticResource FormInput}" Margin="0,2,0,10"/></StackPanel>
                        <StackPanel Grid.Column="2"><TextBlock Text="Email" Style="{StaticResource SubtleText}"/><TextBox x:Name="UEmail" Style="{StaticResource FormInput}" Margin="0,2,0,10"/></StackPanel>
                    </Grid>
                    <TextBlock x:Name="TxtError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="CloseOverlays"/>
                        <Button Content="Save" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSave_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <!-- Reset password overlay -->
        <Border x:Name="OverlayReset" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="360" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock x:Name="TxtResetFor" Text="Reset Password" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,12"/>
                    <TextBlock Text="New password *" Style="{StaticResource SubtleText}"/>
                    <PasswordBox x:Name="RPassword" Height="38" Margin="0,2,0,10"/>
                    <TextBlock x:Name="TxtResetError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="CloseOverlays"/>
                        <Button Content="Reset" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnDoReset_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
