<UserControl x:Class="UMCMS.App.Views.PatientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="980">
    <Grid>
        <!-- Main list -->
        <DockPanel>
            <!-- Toolbar -->
            <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button x:Name="BtnAdd" Content="＋  Add Patient" Style="{StaticResource PrimaryButton}" Click="BtnAdd_Click"/>
                        <Button x:Name="BtnImport" Content="⤓ Import CSV" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnImport_Click"/>
                        <Button x:Name="BtnEdit" Content="Edit" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnEdit_Click" IsEnabled="False"/>
                        <Button x:Name="BtnArchive" Content="Archive" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnArchive_Click" IsEnabled="False"/>
                        <Button x:Name="BtnEmergency" Content="🚨 Emergency Alert" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnEmergency_Click" IsEnabled="False"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="TxtSearch" Style="{StaticResource FormInput}" Width="280" KeyDown="TxtSearch_KeyDown"/>
                        <Button Content="Search" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnSearch_Click"/>
                        <Button Content="Clear" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnClear_Click"/>
                        <TextBlock x:Name="TxtSearchHint" Text="Search by name, reg. no or phone" Style="{StaticResource SubtleText}"
                                   VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource Card}" Padding="0">
                <DataGrid x:Name="Grid" SelectionChanged="Grid_SelectionChanged" MouseDoubleClick="Grid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding PatientType}" Width="90"/>
                        <DataGridTextColumn Header="Reg. No" Binding="{Binding RegistrationNumber}" Width="120"/>
                        <DataGridTextColumn Header="Faculty" Binding="{Binding Faculty}" Width="160"/>
                        <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="80"/>
                        <DataGridTextColumn Header="Age" Binding="{Binding Age}" Width="60"/>
                        <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="130"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </DockPanel>

        <!-- Editor overlay -->
        <Border x:Name="EditorOverlay" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="560" MaxHeight="600" VerticalAlignment="Center" HorizontalAlignment="Center">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock x:Name="TxtEditorTitle" Style="{StaticResource H1}" Text="Add Patient" FontSize="18"/>
                        <TextBlock Text="Fields marked * are required." Style="{StaticResource SubtleText}" Margin="0,2,0,14"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="14"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0">
                                <TextBlock Text="First name *" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FFirst" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="2">
                                <TextBlock Text="Last name *" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FLast" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0">
                                <TextBlock Text="Date of birth" Style="{StaticResource SubtleText}"/>
                                <DatePicker x:Name="FDob" Height="38" Margin="0,2,0,10"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="2">
                                <TextBlock Text="Gender" Style="{StaticResource SubtleText}"/>
                                <ComboBox x:Name="FGender" Height="38" Margin="0,2,0,10">
                                    <ComboBoxItem Content="Male"/><ComboBoxItem Content="Female"/><ComboBoxItem Content="Other"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0">
                                <TextBlock Text="Phone" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FPhone" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="2">
                                <TextBlock Text="Email" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FEmail" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>

                            <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3">
                                <TextBlock Text="Address" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FAddress" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>

                            <StackPanel Grid.Row="4" Grid.Column="0">
                                <TextBlock Text="Patient type" Style="{StaticResource SubtleText}"/>
                                <ComboBox x:Name="FType" Height="38" Margin="0,2,0,10" SelectionChanged="FType_SelectionChanged">
                                    <ComboBoxItem Content="External"/><ComboBoxItem Content="Internal"/>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Grid.Row="4" Grid.Column="2">
                                <TextBlock Text="Reg. number (students)" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FReg" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>

                            <StackPanel x:Name="PanelFaculty" Grid.Row="5" Grid.Column="0">
                                <TextBlock Text="Faculty" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FFaculty" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>
                            <StackPanel x:Name="PanelYear" Grid.Row="5" Grid.Column="2">
                                <TextBlock Text="Year of study" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FYear" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>

                            <StackPanel Grid.Row="6" Grid.Column="0">
                                <TextBlock Text="Emergency contact (parent/guardian)" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FEcName" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>
                            <StackPanel Grid.Row="6" Grid.Column="2">
                                <TextBlock Text="Emergency contact phone" Style="{StaticResource SubtleText}"/>
                                <TextBox x:Name="FEcPhone" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock x:Name="TxtError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,2,0,8"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                            <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                            <Button x:Name="BtnSave" Content="Save Patient" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSave_Click"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Border>
    </Grid>
</UserControl>
