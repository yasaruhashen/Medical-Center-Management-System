<UserControl x:Class="UMCMS.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UMCMS.App.Views"
             mc:Ignorable="d"
             d:DesignHeight="760" d:DesignWidth="1180">

    <UserControl.Resources>
        <SolidColorBrush x:Key="DeepRedBrush"     Color="#8A0007"/>
        <SolidColorBrush x:Key="DarkMaroonBrush"  Color="#4E0205"/>
        <SolidColorBrush x:Key="GoldBrush"        Color="#F7AA37"/>

        <LinearGradientBrush x:Key="RightPanelGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#8A0007" Offset="0.0"/>
            <GradientStop Color="#4E0205" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="InputFieldStyle" TargetType="TextBox">
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="14,0,14,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="#8A0007"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" Focusable="False"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#8A0007"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="border" Property="Background" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PasswordFieldStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="14,0,14,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="#8A0007"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" Focusable="False"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#8A0007"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="border" Property="Background" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#8A0007"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#F7AA37"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#4E0205"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="btnBorder" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoleTabStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="tabBorder" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="tabBorder" Property="Background" Value="#F0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8A0007"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}"
                                   FontSize="{TemplateBinding FontSize}" TextDecorations="Underline"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: login card -->
        <Border Grid.Column="0" Background="#FFFFFF">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="40,30,0,30">
                    <Image Source="/resources/logo.png" Width="64" Height="64"/>
                    <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="MEDI HELP J'Pura" FontSize="18" FontWeight="Bold" Foreground="#4E0205"/>
                        <TextBlock Text="University Medical Centre" FontSize="10" Foreground="Gray"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="48,0,48,0" VerticalAlignment="Center">
                    <TextBlock Text="Welcome Back" FontSize="28" FontWeight="Bold" Foreground="#4E0205" Margin="0,0,0,4"/>
                    <TextBlock Text="Sign in to your account to continue" FontSize="13" Foreground="#777777" Margin="0,0,0,28"/>

                    <Border Background="#F5F5F5" CornerRadius="8" Padding="4" Margin="0,0,0,28">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="BtnRoleAdmin"  Grid.Column="0" Content="Admin"     Style="{StaticResource RoleTabStyle}" Click="BtnRoleAdmin_Click"/>
                            <Button x:Name="BtnRoleDoctor" Grid.Column="1" Content="Doctor"    Style="{StaticResource RoleTabStyle}" Click="BtnRoleDoctor_Click" Margin="4,0,0,0"/>
                            <Button x:Name="BtnRoleStaff"  Grid.Column="2" Content="Reception" Style="{StaticResource RoleTabStyle}" Click="BtnRoleStaff_Click" Margin="4,0,0,0"/>
                            <Button x:Name="BtnRolePharma" Grid.Column="3" Content="Pharmacy"  Style="{StaticResource RoleTabStyle}" Click="BtnRolePharma_Click" Margin="4,0,0,0"/>
                        </Grid>
                    </Border>

                    <TextBlock Text="Username" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,18">
                        <TextBox x:Name="TxtUsername" Style="{StaticResource InputFieldStyle}" Padding="44,0,14,0" KeyDown="TxtUsername_KeyDown"/>
                        <TextBlock Text="👤" FontSize="16" VerticalAlignment="Center" Margin="14,0,0,0" IsHitTestVisible="False"/>
                        <TextBlock x:Name="TxtUsernamePlaceholder" Text="Enter your username" FontSize="13" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="46,0,0,0" IsHitTestVisible="False"/>
                    </Grid>

                    <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,8">
                        <PasswordBox x:Name="TxtPassword" Style="{StaticResource PasswordFieldStyle}" Padding="44,0,14,0" PasswordChanged="TxtPassword_PasswordChanged" KeyDown="TxtPassword_KeyDown"/>
                        <TextBlock Text="🔒" FontSize="16" VerticalAlignment="Center" Margin="14,0,0,0" IsHitTestVisible="False"/>
                        <TextBlock x:Name="TxtPasswordPlaceholder" Text="Enter your password" FontSize="13" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="46,0,0,0" IsHitTestVisible="False"/>
                    </Grid>

                    <DockPanel Margin="0,0,0,24">
                        <Button Content="Forgot Password?" Style="{StaticResource LinkButtonStyle}" DockPanel.Dock="Right" Click="BtnForgotPassword_Click"/>
                    </DockPanel>

                    <Border x:Name="ErrorBorder" Background="#FFF0F0" BorderBrush="#8A0007" BorderThickness="1" CornerRadius="6" Padding="12,10" Margin="0,0,0,14" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠  " FontSize="13" Foreground="#8A0007" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtErrorMessage" Text="Invalid username or password." FontSize="13" Foreground="#8A0007" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <Button x:Name="BtnLogin" Content="Sign In" Style="{StaticResource LoginButtonStyle}" Click="BtnLogin_Click"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <TextBlock Text="Signing in as: " FontSize="12" Foreground="#888888"/>
                        <TextBlock x:Name="TxtCurrentRole" Text="Admin" FontSize="12" FontWeight="SemiBold" Foreground="#8A0007"/>
                    </StackPanel>

                    <TextBlock Text="Demo: admin/admin123 · gdoctor/doc123 · dentist/dent123 · reception/staff123 · pharmacist/pharm123"
                               FontSize="10" Foreground="#BBBBBB" TextWrapping="Wrap" Margin="0,14,0,0" HorizontalAlignment="Center" TextAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Row="4" Text="© 2025 University of Sri Jayewardenepura Medical Centre" FontSize="11" Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            </Grid>
        </Border>

        <!-- RIGHT: brand panel -->
        <Border Grid.Column="1" Background="{StaticResource RightPanelGradient}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="48,0">
                    <Border Width="120" Height="120" CornerRadius="60" Background="#FFFFFF" HorizontalAlignment="Center" Margin="0,0,0,28">
                        <Image Source="/resources/logo.png" Margin="-20,-34,-20,-8"/>
                    </Border>
                    <TextBlock Text="M E D I   H E L P   J'PURA" FontSize="32" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                    <TextBlock Text="University Medical Centre System" FontSize="15" Foreground="#FFDDAA" HorizontalAlignment="Center" Margin="0,0,0,36"/>
                    <Border Height="1" Background="#44FFFFFF" Margin="40,0,40,36"/>
                    <StackPanel Margin="70,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="32" Height="32" CornerRadius="16" Background="#33FFFFFF" Margin="0,0,14,0">
                                <TextBlock Text="🏥" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Patient &amp; Records" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                                <TextBlock Text="Complete EHR, dental &amp; prescriptions" FontSize="11" Foreground="#FFCCAA"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="32" Height="32" CornerRadius="16" Background="#33FFFFFF" Margin="0,0,14,0">
                                <TextBlock Text="📦" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Inventory &amp; Forecast" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                                <TextBlock Text="FEFO dispensing &amp; reorder prediction" FontSize="11" Foreground="#FFCCAA"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Width="32" Height="32" CornerRadius="16" Background="#33FFFFFF" Margin="0,0,14,0">
                                <TextBlock Text="📊" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Reports &amp; Analytics" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFF"/>
                                <TextBlock Text="PDF / Excel / CSV exports" FontSize="11" Foreground="#FFCCAA"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
                <Border Grid.Row="2" VerticalAlignment="Bottom" Height="5" Background="#F7AA37"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
