<UserControl x:Class="UMCMS.App.Views.WaitlistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <Grid>
        <DockPanel>
            <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Content="＋  Add to Waitlist" Style="{StaticResource PrimaryButton}" Click="BtnAdd_Click"/>
                        <Button x:Name="BtnOffer" Content="Offer Slot" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnOffer_Click" IsEnabled="False"/>
                        <Button x:Name="BtnAccept" Content="Accept" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnAccept_Click" IsEnabled="False"/>
                        <Button x:Name="BtnDecline" Content="Decline" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnDecline_Click" IsEnabled="False"/>
                        <Button x:Name="BtnRemove" Content="Remove" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnRemove_Click" IsEnabled="False"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Stream:" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox x:Name="FFilter" Height="38" Width="160" SelectionChanged="Filter_Changed">
                            <ComboBoxItem Content="All" IsSelected="True"/>
                            <ComboBoxItem Content="General"/>
                            <ComboBoxItem Content="Dental"/>
                        </ComboBox>
                        <TextBlock x:Name="TxtNote" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="14,0,0,0"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource Card}" Padding="0">
                <DataGrid x:Name="Grid" AutoGenerateColumns="True" AutoGeneratingColumn="Grid_AutoGeneratingColumn" SelectionChanged="Grid_SelectionChanged"/>
            </Border>
        </DockPanel>

        <Border x:Name="Overlay" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="420" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Add to Waitlist" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,14"/>
                    <TextBlock Text="Patient *" Style="{StaticResource SubtleText}"/>
                    <ComboBox x:Name="FPatient" Height="38" Margin="0,2,0,10" DisplayMemberPath="FullName"/>
                    <TextBlock Text="Stream *" Style="{StaticResource SubtleText}"/>
                    <ComboBox x:Name="FStream" Height="38" Margin="0,2,0,10">
                        <ComboBoxItem Content="General" IsSelected="True"/>
                        <ComboBoxItem Content="Dental"/>
                    </ComboBox>
                    <TextBlock Text="Priority (1 = highest)" Style="{StaticResource SubtleText}"/>
                    <ComboBox x:Name="FPriority" Height="38" Margin="0,2,0,10">
                        <ComboBoxItem Content="1"/><ComboBoxItem Content="2"/><ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/><ComboBoxItem Content="5" IsSelected="True"/>
                    </ComboBox>
                    <TextBlock x:Name="TxtError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                        <Button Content="Add" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSave_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
