<UserControl x:Class="UMCMS.App.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="Generate Report" Style="{StaticResource H2}" Margin="0,0,0,12"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/><ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="*"/><ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Report type" Style="{StaticResource SubtleText}"/>
                        <ComboBox x:Name="FType" Height="38" Margin="0,2,0,0" SelectionChanged="FType_Changed">
                            <ComboBoxItem Content="Inventory Stock" IsSelected="True"/>
                            <ComboBoxItem Content="Low Stock"/>
                            <ComboBoxItem Content="Appointments (date range)"/>
                            <ComboBoxItem Content="Expenditure per Faculty"/>
                            <ComboBoxItem Content="Top Consumed Drugs"/>
                            <ComboBoxItem Content="Quarterly Expenditure"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="2" x:Name="PanelStream">
                        <TextBlock Text="Stream" Style="{StaticResource SubtleText}"/>
                        <ComboBox x:Name="FStream" Height="38" Margin="0,2,0,0">
                            <ComboBoxItem Content="All" IsSelected="True"/><ComboBoxItem Content="General"/><ComboBoxItem Content="Dental"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="2" x:Name="PanelFrom" Visibility="Collapsed">
                        <TextBlock Text="From" Style="{StaticResource SubtleText}"/>
                        <DatePicker x:Name="FFrom" Height="38" Margin="0,2,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="4" x:Name="PanelTo" Visibility="Collapsed">
                        <TextBlock Text="To" Style="{StaticResource SubtleText}"/>
                        <DatePicker x:Name="FTo" Height="38" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                    <Button Content="Export PDF" Style="{StaticResource PrimaryButton}" Click="Export_Click" Tag="PDF"/>
                    <Button Content="Export Excel" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="Export_Click" Tag="Excel"/>
                    <Button Content="Export CSV" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="Export_Click" Tag="CSV"/>
                    <TextBlock x:Name="TxtStatus" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="16,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource Card}" Padding="0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Recently generated" Style="{StaticResource H2}" Margin="14,12,0,8"/>
                <DataGrid x:Name="RecentGrid" AutoGenerateColumns="True"/>
            </DockPanel>
        </Border>
    </DockPanel>
</UserControl>
