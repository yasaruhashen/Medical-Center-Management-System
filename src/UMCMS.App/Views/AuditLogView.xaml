<UserControl x:Class="UMCMS.App.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="Refresh" Style="{StaticResource SecondaryButton}" Click="BtnRefresh_Click"/>
                <StackPanel>
                    <TextBlock Text="Audit Log" Style="{StaticResource H2}"/>
                    <TextBlock Text="Security-relevant and clinical actions, most recent first." Style="{StaticResource SubtleText}"/>
                </StackPanel>
            </DockPanel>
        </Border>
        <Border Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" AutoGenerateColumns="True"/>
        </Border>
    </DockPanel>
</UserControl>
