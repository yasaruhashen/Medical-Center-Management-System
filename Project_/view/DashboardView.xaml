<UserControl x:Class="Project_.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F9FAFB">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Header -->
            <TextBlock x:Name="TxtGreeting" Style="{StaticResource H1}" Text="Welcome" FontWeight="Bold"/>
            <TextBlock x:Name="TxtSub" Style="{StaticResource SubtleText}" Margin="0,2,0,20"/>

            <!-- 4 Stat Cards -->
            <UniformGrid Columns="4" Margin="0,0,0,20">
                <!-- Today's Patients -->
                <Border Style="{StaticResource Card}" Margin="0,0,10,0" Padding="15">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="45" Height="45" CornerRadius="10" Background="#FFF0F0" Margin="0,0,12,0">
                            <TextBlock Text="📅" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Today's Patients" Style="{StaticResource SubtleText}"/>
                            <TextBlock x:Name="TxtTodayAppts" Text="0" FontSize="24" FontWeight="Bold" Foreground="#8A0007"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <!-- Pending Consultations -->
                <Border Style="{StaticResource Card}" Margin="0,0,10,0" Padding="15">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="45" Height="45" CornerRadius="10" Background="#FFFBEB" Margin="0,0,12,0">
                            <TextBlock Text="⏳" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Pending Consultations" Style="{StaticResource SubtleText}"/>
                            <TextBlock x:Name="TxtPending" Text="0" FontSize="24" FontWeight="Bold" Foreground="#D97706"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <!-- Prescriptions Issued -->
                <Border Style="{StaticResource Card}" Margin="0,0,10,0" Padding="15">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="45" Height="45" CornerRadius="10" Background="#FDF0F0" Margin="0,0,12,0">
                            <TextBlock Text="📝" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Prescriptions Issued" Style="{StaticResource SubtleText}"/>
                            <TextBlock x:Name="TxtMyRx" Text="0" FontSize="24" FontWeight="Bold" Foreground="#4E0205"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <!-- Total Appointments -->
                <Border Style="{StaticResource Card}" Padding="15">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="45" Height="45" CornerRadius="10" Background="#F0FDF4" Margin="0,0,12,0">
                            <TextBlock Text="👥" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Total Appointments" Style="{StaticResource SubtleText}"/>
                            <TextBlock x:Name="TxtTotalAppts" Text="0" FontSize="24" FontWeight="Bold" Foreground="#15803D"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- 2-Column Section -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Today's Schedule -->
                <Border Grid.Column="0" Style="{StaticResource Card}" Padding="20">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Today's Schedule" FontSize="16" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,15"/>
                        <TextBlock x:Name="TxtEmptySchedule" Text="No more appointments today!" TextAlignment="Center" Foreground="#9CA3AF" Visibility="Collapsed" Margin="0,30,0,0" DockPanel.Dock="Top"/>
                        <ItemsControl x:Name="TodayScheduleList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#F3F4F6" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,8" Background="White">
                                        <DockPanel>
                                            <!-- Time -->
                                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="⏱ " Foreground="#9CA3AF" FontSize="12"/>
                                                <TextBlock Text="{Binding Time}" Foreground="#4B5563" FontWeight="SemiBold" FontSize="13"/>
                                            </StackPanel>
                                            <!-- Patient Info -->
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Border Width="32" Height="32" CornerRadius="16" Background="#8A0007" Margin="0,0,10,0">
                                                    <TextBlock Text="{Binding Initial}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding PatientName}" FontWeight="SemiBold" Foreground="#1F2937" FontSize="14"/>
                                                    <TextBlock Text="{Binding Details}" Foreground="#9CA3AF" FontSize="12"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DockPanel>
                </Border>

                <!-- This Week's Appointments (Chart representation) -->
                <Border Grid.Column="2" Style="{StaticResource Card}" Padding="20">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="This Week's Appointments" FontSize="16" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,15"/>
                        <!-- Simple Bar Chart -->
                        <ItemsControl x:Name="WeekChartList" VerticalAlignment="Bottom" Height="200">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid VerticalAlignment="Bottom" Margin="2,0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Border Grid.Row="0" Background="#8A0007" CornerRadius="4,4,0,0" Height="{Binding BarHeight}" VerticalAlignment="Bottom" ToolTip="{Binding Count}"/>
                                        <TextBlock Grid.Row="1" Text="{Binding Day}" TextAlignment="Center" Foreground="#6B7280" FontSize="12" Margin="0,8,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- Recent Prescriptions -->
            <Border Style="{StaticResource Card}" Padding="0">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Padding="20,15">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#8A0007" Offset="0.0"/>
                                <GradientStop Color="#4E0205" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="Recent Prescriptions" Foreground="White" FontWeight="SemiBold" FontSize="15"/>
                    </Border>
                    <TextBlock x:Name="TxtEmptyRx" Text="No prescriptions issued yet." TextAlignment="Center" Foreground="#9CA3AF" Visibility="Collapsed" Margin="0,30,0,30" DockPanel.Dock="Top"/>
                    <DataGrid x:Name="RecentRxGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="#F9FAFB" BorderThickness="0" GridLinesVisibility="None" RowHeight="40">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Patient" Binding="{Binding PatientName}" Width="2*" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*"/>
                            <DataGridTextColumn Header="Diagnosis" Binding="{Binding Diagnosis}" Width="2*"/>
                            <DataGridTextColumn Header="Items" Binding="{Binding ItemsCount}" Width="*"/>
                            <DataGridTemplateColumn Header="Status" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding StatusBg}" CornerRadius="12" Padding="8,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Status}" Foreground="{Binding StatusFg}" FontSize="12" FontWeight="SemiBold"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
