<UserControl x:Class="Project_.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" SizeChanged="ScrollViewer_SizeChanged">
        <StackPanel>
            <TextBlock x:Name="TxtGreeting" Style="{StaticResource H1}" Text="Welcome"/>
            <TextBlock x:Name="TxtSub" Style="{StaticResource SubtleText}" Margin="0,2,0,16"/>

            <!-- Responsive: column count is recomputed from the available width -->
            <UniformGrid x:Name="CardsPanel" Rows="0"/>

            <TextBlock x:Name="DocStatusTitle" Style="{StaticResource H2}" Text="Doctor availability" Margin="0,8,0,6" Visibility="Collapsed"/>
            <WrapPanel x:Name="DocStatusPanel" Margin="0,0,0,4"/>

            <UniformGrid x:Name="ChartsPanel" Margin="0,4,0,0"/>

            <Border Style="{StaticResource Card}" Padding="0" Margin="0,18,0,0">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource CardHeader}">
                        <TextBlock x:Name="TxtListTitle" Style="{StaticResource CardHeaderTitle}"/>
                    </Border>
                    <TextBlock x:Name="TxtEmpty" DockPanel.Dock="Bottom" Style="{StaticResource SubtleText}" Visibility="Collapsed" Margin="14,10"/>
                    <DataGrid x:Name="ListGrid" MaxHeight="320" AutoGenerateColumns="True"/>
                </DockPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
