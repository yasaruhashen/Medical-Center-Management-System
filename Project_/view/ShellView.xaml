<UserControl x:Class="Project_.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1100">

    <UserControl.Resources>
        <LinearGradientBrush x:Key="RailGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#8A0007" Offset="0.0"/>
            <GradientStop Color="#4E0205" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- Navigation rail button -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="46"/>
            <Setter Property="Margin" Value="10,3"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#FFE8D8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="navBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="14,0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="navBorder" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="ACTIVE">
                                <Setter TargetName="navBorder" Property="Background" Value="#FFFFFF"/>
                                <Setter Property="Foreground" Value="#8A0007"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Margin" Value="14,8,14,16"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#F7AA37"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ── LEFT NAV RAIL ── -->
        <Border Grid.Column="0" Background="{StaticResource RailGradient}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Brand -->
                <StackPanel Orientation="Horizontal" Margin="18,22,0,22">
                    <Image Source="/resources/logo.png" Width="40" Height="40"/>
                    <StackPanel VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="MEDI HELP" FontSize="15" FontWeight="Bold"
                                   Foreground="White" FontFamily="Segoe UI"/>
                        <TextBlock Text="J'Pura Medical Centre" FontSize="10"
                                   Foreground="#FFD9B8"/>
                    </StackPanel>
                </StackPanel>

                <!-- Nav items (populated in code-behind, role-scoped) -->
                <StackPanel x:Name="NavPanel" Grid.Row="1" Margin="0,6,0,0"/>

                <!-- Logout -->
                <Button x:Name="BtnLogout" Grid.Row="2" Content="⏻  Sign Out"
                        Style="{StaticResource LogoutButtonStyle}" Click="BtnLogout_Click"/>
            </Grid>
        </Border>

        <!-- ── RIGHT: HEADER + CONTENT ── -->
        <Grid Grid.Column="1" Background="#F8FAFC">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top bar -->
            <Border Grid.Row="0" Background="White" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
                <Grid Margin="24,14">
                    <TextBlock x:Name="TxtPageTitle" Text="Dashboard"
                               FontSize="20" FontWeight="SemiBold" Foreground="#4E0205"
                               VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                            <TextBlock x:Name="TxtUserName" Text="User" FontSize="13"
                                       FontWeight="SemiBold" Foreground="#333" HorizontalAlignment="Right"/>
                            <Border Background="#FBE9E9" CornerRadius="4" Padding="6,1" HorizontalAlignment="Right" Margin="0,2,0,0">
                                <TextBlock x:Name="TxtUserRole" Text="Role" FontSize="10"
                                           Foreground="#8A0007"/>
                            </Border>
                        </StackPanel>
                        <Border Width="40" Height="40" CornerRadius="20" Background="#8A0007">
                            <TextBlock x:Name="TxtUserInitials" Text="U" Foreground="White"
                                       FontSize="15" FontWeight="SemiBold"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content host -->
            <ContentControl x:Name="ContentHost" Grid.Row="1" Margin="24"/>
        </Grid>
    </Grid>
</UserControl>
