<UserControl x:Class="Project_.Views.ConsultationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F9FAFB">
    <DockPanel Margin="20">
        <!-- Header -->
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Prescriptions" Style="{StaticResource H1}" FontWeight="Bold" Margin="0,0,0,2"/>
                <TextBlock Text="Write new prescriptions or view your prescription history." Style="{StaticResource SubtleText}"/>
            </StackPanel>
            
            <!-- Tab Toggle -->
            <Border CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1" HorizontalAlignment="Right" VerticalAlignment="Center" Background="White" ClipToBounds="True">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnTabWrite" Content="Write Prescription" Click="BtnTabWrite_Click" BorderThickness="0" Background="#8A0007" Foreground="White" Padding="15,8" FontWeight="SemiBold" Cursor="Hand" />
                    <Button x:Name="BtnTabHistory" Content="History" Click="BtnTabHistory_Click" BorderThickness="0" Background="White" Foreground="#374151" Padding="15,8" Cursor="Hand" />
                </StackPanel>
            </Border>
        </DockPanel>

        <!-- Write Panel -->
        <ScrollViewer x:Name="WritePanel" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,0,20">
                <Border x:Name="MsgPanel" Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="8" Padding="15,10" Margin="0,0,0,15" Visibility="Collapsed">
                    <TextBlock x:Name="TxtMsg" Text="Prescription submitted successfully." Foreground="#166534" FontSize="14"/>
                </Border>
                
                <Border x:Name="ErrorPanel" Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="8" Padding="15,10" Margin="0,0,0,15" Visibility="Collapsed">
                    <TextBlock x:Name="TxtError" Text="Error message." Foreground="#991B1B" FontSize="14"/>
                </Border>

                <!-- Patient & Diagnosis -->
                <Border Style="{StaticResource Card}" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Patient &amp; Diagnosis" FontSize="16" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,15"/>
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Patient *" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                                <ComboBox x:Name="CmbPatient" Style="{StaticResource FormInput}" Height="38" DisplayMemberPath="DisplayValue" SelectedValuePath="Id" SelectionChanged="CmbPatient_SelectionChanged"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Diagnosis" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                                <TextBox x:Name="TxtDiagnosis" Style="{StaticResource FormInput}" Height="38" Padding="10,0" />
                            </StackPanel>
                        </Grid>
                        
                        <Border x:Name="AllergyAlert" Background="#FFFBEB" BorderBrush="#FDE68A" BorderThickness="1" CornerRadius="8" Padding="12,8" Margin="0,0,0,15" Visibility="Collapsed">
                            <TextBlock x:Name="TxtAllergies" Text="Patient has known allergies: " Foreground="#B45309" FontSize="13"/>
                        </Border>

                        <TextBlock Text="Notes / Instructions" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtNotes" Style="{StaticResource FormInput}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="10,8"/>
                    </StackPanel>
                </Border>

                <!-- Medications -->
                <Border Style="{StaticResource Card}" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,15">
                            <TextBlock DockPanel.Dock="Left" Text="Medications" FontSize="16" FontWeight="SemiBold" Foreground="#374151" VerticalAlignment="Center"/>
                            <Button DockPanel.Dock="Right" HorizontalAlignment="Right" Content="+ Add Medication" Click="BtnAddMedication_Click" Style="{StaticResource PrimaryButton}" Padding="12,0" Height="30" FontSize="13"/>
                        </DockPanel>

                        <TextBlock x:Name="TxtEmptyMeds" Text="No medications added yet. Click 'Add Medication' to start." Foreground="#9CA3AF" TextAlignment="Center" Margin="0,30,0,30" Visibility="Collapsed"/>

                        <ItemsControl x:Name="MedsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                        <StackPanel>
                                            <DockPanel Margin="0,0,0,10">
                                                <TextBlock DockPanel.Dock="Left" Text="{Binding IndexLabel}" FontSize="13" FontWeight="SemiBold" Foreground="#4B5563" VerticalAlignment="Center"/>
                                                <Button DockPanel.Dock="Right" HorizontalAlignment="Right" Content="🗑" Background="Transparent" BorderThickness="0" Foreground="#8A0007" Cursor="Hand" Click="BtnRemoveMedication_Click" DataContext="{Binding}"/>
                                            </DockPanel>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*"/>
                                                    <ColumnDefinition Width="15"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="15"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="Select Medicine" FontSize="12" Foreground="#6B7280" Margin="0,0,0,4"/>
                                                    <ComboBox ItemsSource="{Binding AvailableMedicines}" SelectedValuePath="Id" DisplayMemberPath="NameWithStock" SelectedValue="{Binding SelectedMedicineId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="36" Style="{StaticResource FormInput}"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="Dosage *" FontSize="12" Foreground="#6B7280" Margin="0,0,0,4"/>
                                                    <TextBox Text="{Binding Dosage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="36" Style="{StaticResource FormInput}" Padding="8,0"/>
                                                </StackPanel>

                                                <StackPanel Grid.Column="4">
                                                    <TextBlock Text="Quantity *" FontSize="12" Foreground="#6B7280" Margin="0,0,0,4"/>
                                                    <TextBox Text="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="36" Style="{StaticResource FormInput}" Padding="8,0"/>
                                                </StackPanel>
                                            </Grid>

                                            <StackPanel Margin="0,10,0,0">
                                                <TextBlock Text="Instructions *" FontSize="12" Foreground="#6B7280" Margin="0,0,0,4"/>
                                                <TextBox Text="{Binding Instructions, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Height="36" Style="{StaticResource FormInput}" Padding="8,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Actions -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Clear" Click="BtnClear_Click" Style="{StaticResource SecondaryButton}" Width="120"/>
                    <Button Grid.Column="2" Content="✓ Submit Prescription" Click="BtnSubmit_Click" Style="{StaticResource PrimaryButton}"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- History Panel -->
        <ScrollViewer x:Name="HistoryPanel" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
            <StackPanel Margin="0,0,0,20">
                <Border Style="{StaticResource Card}" Padding="5,5,5,5" Margin="0,0,0,15" CornerRadius="8">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Left" Text="🔍" Margin="10,0" VerticalAlignment="Center" Foreground="#9CA3AF"/>
                        <TextBox x:Name="TxtSearchHistory" TextChanged="TxtSearchHistory_TextChanged" BorderThickness="0" Height="36" VerticalContentAlignment="Center" Background="Transparent" Foreground="#374151" FontSize="14"/>
                    </DockPanel>
                </Border>

                <TextBlock x:Name="TxtEmptyHistory" Text="No prescriptions found." TextAlignment="Center" Foreground="#9CA3AF" Margin="0,40,0,0" Visibility="Collapsed"/>

                <ItemsControl x:Name="HistoryList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource Card}" Padding="0" Margin="0,0,0,15">
                                <StackPanel>
                                    <Border Padding="20,12" BorderThickness="0,0,0,1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#FFFBEB"/>
                                                <Setter Property="BorderBrush" Value="#FDE68A"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsProcessed}" Value="True">
                                                        <Setter Property="Background" Value="#F0FDF4"/>
                                                        <Setter Property="BorderBrush" Value="#D1FAE5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <DockPanel>
                                            <Border DockPanel.Dock="Right" CornerRadius="12" Padding="8,2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#FDE68A"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsProcessed}" Value="True">
                                                                <Setter Property="Background" Value="#D1FAE5"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#92400E"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsProcessed}" Value="True">
                                                                    <Setter Property="Foreground" Value="#065F46"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Border>
                                            <StackPanel>
                                                <TextBlock Text="{Binding PatientName}" FontWeight="Bold" Foreground="#1F2937" FontSize="15" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding DiagnosisAndDate}" Foreground="#6B7280" FontSize="13"/>
                                            </StackPanel>
                                        </DockPanel>
                                    </Border>
                                    <ItemsControl ItemsSource="{Binding Items}" Margin="20,15,20,15">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <DockPanel Margin="0,0,0,6">
                                                    <TextBlock DockPanel.Dock="Left" Text="{Binding MedAndDosage}" Foreground="#374151" FontSize="14"/>
                                                    <TextBlock DockPanel.Dock="Right" Text="{Binding QtyAndInstr}" Foreground="#6B7280" FontSize="14" HorizontalAlignment="Right"/>
                                                </DockPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <!-- Notes section -->
                                    <Border BorderThickness="0,1,0,0" BorderBrush="#F3F4F6" Padding="20,10" Visibility="{Binding HasNotes}">
                                        <TextBlock Text="{Binding Notes}" Foreground="#9CA3AF" FontSize="13"/>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
