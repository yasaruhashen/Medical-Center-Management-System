<UserControl x:Class="Project_.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <Grid>
        <DockPanel>
            <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button x:Name="BtnDispense" Content="Dispense" Style="{StaticResource PrimaryButton}" Click="BtnDispense_Click" IsEnabled="False"/>
                        <Button x:Name="BtnAddItem" Content="＋ Item" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnAddItem_Click"/>
                        <Button x:Name="BtnAddBatch" Content="＋ Batch" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Click="BtnAddBatch_Click" IsEnabled="False"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtHeader" Style="{StaticResource H2}" VerticalAlignment="Center"/>
                        <CheckBox x:Name="ChkLowOnly" Content="Low stock only" VerticalAlignment="Center" Margin="18,0,0,0" Click="Filter_Changed"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource Card}" Padding="0">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource CardHeader}">
                        <TextBlock Text="📦  Stock Items" Style="{StaticResource CardHeaderTitle}"/>
                    </Border>
                    <DataGrid x:Name="Grid" AutoGenerateColumns="True" AutoGeneratingColumn="Grid_AutoGeneratingColumn" SelectionChanged="Grid_SelectionChanged"/>
                </DockPanel>
            </Border>
        </DockPanel>

        <!-- Dispense overlay -->
        <Border x:Name="OverlayDispense" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="380" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Dispense Stock" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,6"/>
                    <TextBlock x:Name="TxtDispItem" Style="{StaticResource SubtleText}" Margin="0,0,0,12"/>
                    <TextBlock Text="Quantity *" Style="{StaticResource SubtleText}"/>
                    <TextBox x:Name="FDispQty" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                    <TextBlock x:Name="TxtDispError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="CloseOverlays"/>
                        <Button Content="Dispense (FEFO)" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnDoDispense_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <!-- Add item overlay -->
        <Border x:Name="OverlayItem" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="440" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Add Inventory Item" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,14"/>
                    <TextBlock Text="Item name *" Style="{StaticResource SubtleText}"/>
                    <TextBox x:Name="IName" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Stream *" Style="{StaticResource SubtleText}"/>
                            <ComboBox x:Name="IStream" Height="38" Margin="0,2,0,10">
                                <ComboBoxItem Content="General" IsSelected="True"/><ComboBoxItem Content="Dental"/>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Category" Style="{StaticResource SubtleText}"/>
                            <TextBox x:Name="ICategory" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                        </StackPanel>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Unit" Style="{StaticResource SubtleText}"/>
                            <TextBox x:Name="IUnit" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Unit price" Style="{StaticResource SubtleText}"/>
                            <TextBox x:Name="IPrice" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Reorder level" Style="{StaticResource SubtleText}"/>
                            <TextBox x:Name="IReorder" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock x:Name="TxtItemError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="CloseOverlays"/>
                        <Button Content="Save Item" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSaveItem_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <!-- Add batch overlay -->
        <Border x:Name="OverlayBatch" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Receive Stock Batch" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,6"/>
                    <TextBlock x:Name="TxtBatchItem" Style="{StaticResource SubtleText}" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Quantity *" Style="{StaticResource SubtleText}"/>
                            <TextBox x:Name="BQty" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Purchase price" Style="{StaticResource SubtleText}"/>
                            <TextBox x:Name="BPrice" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="Supplier" Style="{StaticResource SubtleText}"/>
                    <TextBox x:Name="BSupplier" Style="{StaticResource FormInput}" Margin="0,2,0,10"/>
                    <TextBlock Text="Expiry date" Style="{StaticResource SubtleText}"/>
                    <DatePicker x:Name="BExpiry" Height="38" Margin="0,2,0,10"/>
                    <TextBlock x:Name="TxtBatchError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="CloseOverlays"/>
                        <Button Content="Add Batch" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSaveBatch_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
