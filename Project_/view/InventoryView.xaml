<UserControl x:Class="Project_.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F9FAFB">
    <ScrollViewer Padding="25" VerticalScrollBarVisibility="Auto">
        <StackPanel x:Name="MainContainer" Opacity="0">
            <StackPanel.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </StackPanel.Triggers>
            
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Inventory Management" FontWeight="Bold" FontSize="24" Foreground="#1F2937"/>
                    <TextBlock Text="Manage medical supplies, medicines, and stock levels" FontSize="14" Foreground="#6B7280"/>
                </StackPanel>
                <Button x:Name="BtnAddItem" Grid.Column="1" Content="+ Add Item" Background="#8A0007" Foreground="White" FontWeight="SemiBold"
                        Padding="15,10" BorderThickness="0" Click="BtnAddItem_Click" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style></Button.Resources>
                </Button>
            </Grid>

            <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="12">
                <StackPanel>
                    <Border Background="#F9FAFB" CornerRadius="12,12,0,0" Padding="20,15" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="ITEM" FontWeight="SemiBold" Foreground="#6B7280" FontSize="12"/>
                            <TextBlock Grid.Column="1" Text="CATEGORY" FontWeight="SemiBold" Foreground="#6B7280" FontSize="12"/>
                            <TextBlock Grid.Column="2" Text="QUANTITY" FontWeight="SemiBold" Foreground="#6B7280" FontSize="12"/>
                            <TextBlock Grid.Column="3" Text="UNIT PRICE" FontWeight="SemiBold" Foreground="#6B7280" FontSize="12"/>
                            <TextBlock Grid.Column="4" Text="ACTIONS" FontWeight="SemiBold" Foreground="#6B7280" FontSize="12"/>
                        </Grid>
                    </Border>
                    <ItemsControl x:Name="InventoryList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="20,15" BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Background="White">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding ItemName}" Foreground="#1F2937" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Supplier}" Foreground="#6B7280" FontSize="11"/>
                                        </StackPanel>
                                        
                                        <TextBlock Grid.Column="1" Text="{Binding Category}" Foreground="#4B5563" VerticalAlignment="Center"/>
                                        
                                        <TextBlock Grid.Column="2" Text="{Binding Quantity}" Foreground="#4B5563" VerticalAlignment="Center" FontWeight="Bold"/>
                                        
                                        <TextBlock Grid.Column="3" Text="{Binding UnitPrice}" Foreground="#4B5563" VerticalAlignment="Center"/>
                                        
                                        <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Content="✏️" Tag="{Binding Id}" Click="BtnEditItem_Click" Background="Transparent" BorderThickness="0" Margin="0,0,10,0" Cursor="Hand"/>
                                            <Button Content="🗑️" Tag="{Binding Id}" Click="BtnDeleteItem_Click" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
