<UserControl x:Class="Project_.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="640" d:DesignWidth="1000">
    <Grid>
        <DockPanel>
            <Border DockPanel.Dock="Top" Style="{StaticResource Card}" Padding="14" Margin="0,0,0,14">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button x:Name="BtnSubscribe" Content="🔔 Availability Alert" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="BtnSubscribe_Click"/>
                        <Button x:Name="BtnBook" Content="＋  Book Appointment" Style="{StaticResource PrimaryButton}" Click="BtnBook_Click"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Day:" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker x:Name="FDay" Height="38" Width="160" SelectedDateChanged="Filter_Changed"/>
                        <CheckBox x:Name="ChkAllDays" Content="All days" VerticalAlignment="Center" Margin="14,0,0,0" Click="Filter_Changed"/>
                        <Button x:Name="BtnToggleView" Content="📅 Day View" Style="{StaticResource SecondaryButton}" Margin="14,0,0,0" Click="BtnToggleView_Click"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border x:Name="ActionBar" DockPanel.Dock="Bottom" Style="{StaticResource Card}" Padding="12" Margin="0,14,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Selected appointment:" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button x:Name="BtnCheckIn" Content="Check In" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Status_Click" Tag="CheckedIn" IsEnabled="False"/>
                    <Button x:Name="BtnComplete" Content="Complete" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Status_Click" Tag="Completed" IsEnabled="False"/>
                    <Button x:Name="BtnNoShow" Content="No-Show" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Status_Click" Tag="NoShow" IsEnabled="False"/>
                    <Button x:Name="BtnReschedule" Content="Reschedule" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="BtnReschedule_Click" IsEnabled="False"/>
                    <Button x:Name="BtnCancelAppt" Content="Cancel" Style="{StaticResource SecondaryButton}" Click="Status_Click" Tag="Cancelled" IsEnabled="False"/>
                </StackPanel>
            </Border>

            <Grid>
                <Border x:Name="ListHost" Style="{StaticResource Card}" Padding="0">
                    <DataGrid x:Name="Grid" AutoGenerateColumns="True" AutoGeneratingColumn="Grid_AutoGeneratingColumn"
                              SelectionChanged="Grid_SelectionChanged"/>
                </Border>
                <Border x:Name="DayHost" Style="{StaticResource Card}" Visibility="Collapsed">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="DayPanel" Orientation="Horizontal"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </DockPanel>

        <!-- Booking overlay -->
        <Border x:Name="Overlay" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="480" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Book Appointment" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,14"/>

                    <TextBlock Text="Patient *" Style="{StaticResource SubtleText}"/>
                    <ComboBox x:Name="FPatient" Height="38" Margin="0,2,0,10" DisplayMemberPath="FullName"/>

                    <TextBlock Text="Doctor *" Style="{StaticResource SubtleText}"/>
                    <ComboBox x:Name="FDoctor" Height="38" Margin="0,2,0,10" DisplayMemberPath="FullName" SelectionChanged="FDoctor_SelectionChanged"/>
                    <TextBlock x:Name="TxtStreamHint" Style="{StaticResource SubtleText}" Margin="0,0,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Date *" Style="{StaticResource SubtleText}"/>
                            <DatePicker x:Name="FDate" Height="38" Margin="0,2,0,10"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Time *" Style="{StaticResource SubtleText}"/>
                            <ComboBox x:Name="FTime" Height="38" Margin="0,2,0,10"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Duration (min)" Style="{StaticResource SubtleText}"/>
                            <ComboBox x:Name="FDuration" Height="38" Margin="0,2,0,10"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Notes" Style="{StaticResource SubtleText}"/>
                    <TextBox x:Name="FNotes" Style="{StaticResource FormInput}" Height="60" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top" Margin="0,2,0,10"/>

                    <TextBlock x:Name="TxtError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8" TextWrapping="Wrap"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="BtnCancelBook_Click"/>
                        <Button Content="Book" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSaveBook_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <!-- Reschedule overlay -->
        <Border x:Name="OverlayResched" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="420" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Reschedule Appointment" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,4"/>
                    <TextBlock x:Name="TxtReschedFor" Style="{StaticResource SubtleText}" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="New date *" Style="{StaticResource SubtleText}"/>
                            <DatePicker x:Name="FReDate" Height="38" Margin="0,2,0,10"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="New time *" Style="{StaticResource SubtleText}"/>
                            <ComboBox x:Name="FReTime" Height="38" Margin="0,2,0,10"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock x:Name="TxtReschedError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="BtnCancelResched_Click"/>
                        <Button Content="Reschedule" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSaveResched_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>

        <!-- Availability subscription overlay -->
        <Border x:Name="OverlaySub" Background="#88000000" Visibility="Collapsed">
            <Border Style="{StaticResource Card}" Width="420" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Doctor Availability Alert" Style="{StaticResource H1}" FontSize="18" Margin="0,0,0,4"/>
                    <TextBlock Text="Notify a patient when a doctor returns on-site." Style="{StaticResource SubtleText}" Margin="0,0,0,12"/>
                    <TextBlock Text="Patient *" Style="{StaticResource SubtleText}"/>
                    <ComboBox x:Name="SPatient" Height="38" Margin="0,2,0,10" DisplayMemberPath="FullName"/>
                    <TextBlock Text="Doctor *" Style="{StaticResource SubtleText}"/>
                    <ComboBox x:Name="SDoctor" Height="38" Margin="0,2,0,10" DisplayMemberPath="FullName"/>
                    <TextBlock x:Name="TxtSubError" Foreground="#8A0007" FontSize="12" Visibility="Collapsed" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Click="BtnCancelSub_Click"/>
                        <Button Content="Subscribe" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnSaveSub_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>
