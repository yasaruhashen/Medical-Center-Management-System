<UserControl x:Class="Project_.Views.AdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_.Views"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800"
             Background="#F9FAFB">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="25">
        <StackPanel x:Name="MainContainer" Opacity="0">
            <StackPanel.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.6" />
                            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,30,0,0" To="0,0,0,0" Duration="0:0:0.6">
                                <ThicknessAnimation.EasingFunction>
                                    <QuadraticEase EasingMode="EaseOut"/>
                                </ThicknessAnimation.EasingFunction>
                            </ThicknessAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </StackPanel.Triggers>
            
            <!-- Header -->
            <TextBlock Text="Admin Dashboard" FontWeight="Bold" FontSize="24" Foreground="#1F2937" Margin="0,0,0,5"/>
            <TextBlock Text="MEDI HELP J'PURA — System Overview" FontSize="14" Foreground="#6B7280" Margin="0,0,0,25"/>

            <!-- Stats Grid -->
            <Grid Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Total Patients -->
                <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource StatCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{DynamicResource IconContainerStyle}" Background="#FFF0F0">
                            <!-- Icon Placeholder -->
                            <TextBlock Text="👥" FontSize="20" Foreground="#8A0007" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="Total Patients" FontSize="12" Foreground="#6B7280"/>
                            <TextBlock x:Name="TotalPatientsTxt" Text="0" FontSize="24" FontWeight="Bold" Foreground="#8A0007"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Doctors -->
                <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource StatCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{DynamicResource IconContainerStyle}" Background="#FDF0F0">
                            <TextBlock Text="👨‍⚕️" FontSize="20" Foreground="#4E0205" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="Doctors" FontSize="12" Foreground="#6B7280"/>
                            <TextBlock x:Name="TotalDoctorsTxt" Text="0" FontSize="24" FontWeight="Bold" Foreground="#4E0205"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Staff -->
                <Border Grid.Row="0" Grid.Column="4" Style="{DynamicResource StatCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{DynamicResource IconContainerStyle}" Background="#FFFBF0">
                            <TextBlock Text="👩‍💻" FontSize="20" Foreground="#F7AA37" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="Staff Members" FontSize="12" Foreground="#6B7280"/>
                            <TextBlock x:Name="TotalStaffTxt" Text="0" FontSize="24" FontWeight="Bold" Foreground="#F7AA37"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Pending Prescriptions -->
                <Border Grid.Row="2" Grid.Column="0" Style="{DynamicResource StatCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{DynamicResource IconContainerStyle}" Background="#FDF0F0">
                            <TextBlock Text="📝" FontSize="20" Foreground="#4E0205" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="Pending Prescripts." FontSize="11" Foreground="#6B7280"/>
                            <TextBlock x:Name="PendingPrescriptsTxt" Text="0" FontSize="24" FontWeight="Bold" Foreground="#4E0205"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Low Stock -->
                <Border Grid.Row="2" Grid.Column="2" Style="{DynamicResource StatCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{DynamicResource IconContainerStyle}" Background="#FFFBEB">
                            <TextBlock Text="⚠️" FontSize="20" Foreground="#D97706" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                            <TextBlock Text="Low Stock Items" FontSize="12" Foreground="#6B7280"/>
                            <TextBlock x:Name="LowStockTxt" Text="0" FontSize="24" FontWeight="Bold" Foreground="#D97706"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Charts Section (Mocked up) -->
            <Grid Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Monthly Patients -->
                <Border Grid.Column="0" Style="{DynamicResource ChartCardStyle}">
                    <StackPanel>
                        <TextBlock Text="📈 Monthly Patients" FontWeight="SemiBold" FontSize="16" Foreground="#374151" Margin="0,0,0,15"/>
                        <Grid Height="200">
                            <!-- Mock lines for the chart background -->
                            <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,20">
                                <Border Height="1" Background="#F0E6E6" Margin="0,0,0,40"/>
                                <Border Height="1" Background="#F0E6E6" Margin="0,0,0,40"/>
                                <Border Height="1" Background="#F0E6E6" Margin="0,0,0,40"/>
                                <Border Height="1" Background="#F0E6E6"/>
                            </StackPanel>
                            <ItemsControl x:Name="PatientsChartList" VerticalAlignment="Bottom">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Height="200"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Bottom" Margin="0,0,15,0">
                                            <Border Width="30" Height="{Binding Value}" Background="#8A0007" CornerRadius="4,4,0,0" VerticalAlignment="Bottom" RenderTransformOrigin="0,1">
                                                <Border.RenderTransform>
                                                    <ScaleTransform ScaleY="0"/>
                                                </Border.RenderTransform>
                                                <Border.Triggers>
                                                    <EventTrigger RoutedEvent="Loaded">
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0" To="1" Duration="0:0:0.8">
                                                                    <DoubleAnimation.EasingFunction><ExponentialEase EasingMode="EaseOut" Exponent="5"/></DoubleAnimation.EasingFunction>
                                                                </DoubleAnimation>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                </Border.Triggers>
                                            </Border>
                                            <TextBlock Text="{Binding Label}" FontSize="11" Foreground="#9CA3AF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </StackPanel>
                </Border>

            </Grid>

            <!-- Inventory by Category -->
            <Border Style="{DynamicResource ChartCardStyle}">
                <StackPanel>
                    <TextBlock Text="📦 Inventory by Category" FontWeight="SemiBold" FontSize="16" Foreground="#374151" Margin="0,0,0,15"/>
                    <Grid Height="200">
                        <!-- Mock lines for the chart background -->
                        <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,20">
                            <Border Height="1" Background="#F0E6E6" Margin="0,0,0,40"/>
                            <Border Height="1" Background="#F0E6E6" Margin="0,0,0,40"/>
                            <Border Height="1" Background="#F0E6E6" Margin="0,0,0,40"/>
                            <Border Height="1" Background="#F0E6E6"/>
                        </StackPanel>
                        <ItemsControl x:Name="InventoryChartList" VerticalAlignment="Bottom">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Height="200"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Bottom" Margin="0,0,15,0">
                                        <Border Width="40" Height="{Binding Value}" Background="#8A0007" CornerRadius="4,4,0,0" VerticalAlignment="Bottom" RenderTransformOrigin="0,1">
                                            <Border.RenderTransform>
                                                <ScaleTransform ScaleY="0"/>
                                            </Border.RenderTransform>
                                            <Border.Triggers>
                                                <EventTrigger RoutedEvent="Loaded">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0" To="1" Duration="0:0:0.8">
                                                                <DoubleAnimation.EasingFunction><ExponentialEase EasingMode="EaseOut" Exponent="5"/></DoubleAnimation.EasingFunction>
                                                            </DoubleAnimation>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Border.Triggers>
                                        </Border>
                                        <TextBlock Text="{Binding Label}" FontSize="11" Foreground="#9CA3AF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>

    <UserControl.Resources>
        <!-- Card Styles -->
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderBrush" Value="#F3F4F6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Color="#000000" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ChartCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="#F3F4F6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Color="#000000" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="IconContainerStyle" TargetType="Border">
            <Setter Property="Width" Value="48"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="CornerRadius" Value="12"/>
        </Style>
    </UserControl.Resources>
</UserControl>
