<UserControl x:Class="Project_.Views.PatientDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F9FAFB">
    <DockPanel Margin="20">
        <!-- Header -->
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
            <TextBlock Text="Patient Details" Style="{StaticResource H1}" FontWeight="Bold" Margin="0,0,0,2"/>
            <TextBlock Text="View patient records, appointment history, and prescriptions." Style="{StaticResource SubtleText}"/>
        </StackPanel>

        <!-- Search Bar -->
        <Border DockPanel.Dock="Top" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1" Background="White" Padding="10,5" Margin="0,0,0,20">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="🔍" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="#9CA3AF"/>
                <TextBox x:Name="TxtSearch" TextChanged="TxtSearch_TextChanged" BorderThickness="0" Background="Transparent" Foreground="#374151" FontSize="14" VerticalContentAlignment="Center"/>
            </DockPanel>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="2.5*"/>
            </Grid.ColumnDefinitions>

            <!-- Patient List -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0" ClipToBounds="True">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Background="#4E0205" Padding="15,12">
                        <TextBlock x:Name="TxtPatientsCount" Text="Patients (0)" Foreground="White" FontWeight="SemiBold" FontSize="14"/>
                    </Border>
                    <TextBlock x:Name="TxtEmptyList" Text="No patients found." TextAlignment="Center" Foreground="#9CA3AF" Margin="0,30,0,0" Visibility="Collapsed" DockPanel.Dock="Top"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="PatientsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="0,0,0,1" BorderBrush="#F9FAFB" Background="{Binding BackgroundBrush}" Cursor="Hand" MouseLeftButtonDown="Patient_Click">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#FFF0F0"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border Padding="15,12">
                                            <DockPanel>
                                                <TextBlock DockPanel.Dock="Right" Text="›" Foreground="#8A0007" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Visibility="{Binding ArrowVisibility}"/>
                                                <Border Width="32" Height="32" CornerRadius="16" Background="#8A0007" Margin="0,0,10,0" DockPanel.Dock="Left">
                                                    <TextBlock Text="{Binding Initial}" Foreground="White" FontWeight="Bold" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}" FontWeight="{Binding NameWeight}" Foreground="#1F2937" FontSize="14"/>
                                                    <TextBlock Text="{Binding Details}" Foreground="#9CA3AF" FontSize="12"/>
                                                </StackPanel>
                                            </DockPanel>
                                        </Border>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- Patient Details -->
            <ScrollViewer x:Name="DetailPanel" Grid.Column="2" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                <StackPanel>
                    <!-- Info Card -->
                    <Border Style="{StaticResource Card}" Padding="0" ClipToBounds="True" Margin="0,0,0,15">
                        <StackPanel>
                            <Border Padding="20">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#8A0007" Offset="0.0"/>
                                        <GradientStop Color="#4E0205" Offset="1.0"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <DockPanel>
                                    <Border DockPanel.Dock="Left" Width="56" Height="56" CornerRadius="28" Background="#33FFFFFF" Margin="0,0,15,0">
                                        <TextBlock x:Name="DetInitial" Text="A" Foreground="White" FontWeight="Bold" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock x:Name="DetName" Text="Patient Name" Foreground="White" FontWeight="Bold" FontSize="18"/>
                                        <TextBlock x:Name="DetSub" Text="ID • Gender" Foreground="#FECACA" FontSize="14"/>
                                        <WrapPanel x:Name="DetTags" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <UniformGrid Columns="3" Margin="0,0,0,0">
                                <Border BorderThickness="0,0,1,0" BorderBrush="#F3F4F6" Padding="15,12">
                                    <StackPanel>
                                        <TextBlock Text="DOB" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="DetDob" Text="-" FontWeight="SemiBold" Foreground="#1F2937" FontSize="14" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <Border BorderThickness="0,0,1,0" BorderBrush="#F3F4F6" Padding="15,12">
                                    <StackPanel>
                                        <TextBlock Text="Phone" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="DetPhone" Text="-" FontWeight="SemiBold" Foreground="#1F2937" FontSize="14" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                <Border Padding="15,12">
                                    <StackPanel>
                                        <TextBlock Text="Registered" Foreground="#9CA3AF" FontSize="12" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="DetRegDate" Text="-" FontWeight="SemiBold" Foreground="#1F2937" FontSize="14" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                            <Border BorderThickness="0,1,0,0" BorderBrush="#F3F4F6" Padding="20,10">
                                <TextBlock x:Name="DetAddress" Text="📍 Address" Foreground="#6B7280" FontSize="12"/>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Appointment History -->
                    <Border Style="{StaticResource Card}" Padding="0" ClipToBounds="True" Margin="0,0,0,15">
                        <StackPanel>
                            <Border Background="#FDF8F8" BorderThickness="0,0,0,1" BorderBrush="#F3F4F6" Padding="20,12">
                                <DockPanel>
                                    <TextBlock DockPanel.Dock="Left" Text="📅 " Foreground="#8A0007"/>
                                    <TextBlock x:Name="TxtApptCount" Text="Appointment History (0)" FontWeight="SemiBold" Foreground="#374151" FontSize="14"/>
                                </DockPanel>
                            </Border>
                            <TextBlock x:Name="TxtEmptyAppts" Text="No appointments found." TextAlignment="Center" Foreground="#9CA3AF" FontSize="14" Margin="0,20,0,20" Visibility="Collapsed"/>
                            <ItemsControl x:Name="ApptHistoryList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderThickness="0,0,0,1" BorderBrush="#F9FAFB" Padding="20,12">
                                            <DockPanel>
                                                <Border DockPanel.Dock="Right" Background="{Binding StatusBg}" CornerRadius="12" Padding="8,2" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Status}" Foreground="{Binding StatusFg}" FontWeight="SemiBold" FontSize="12"/>
                                                </Border>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Reason}" FontWeight="SemiBold" Foreground="#1F2937" FontSize="14"/>
                                                    <TextBlock Text="{Binding Details}" Foreground="#9CA3AF" FontSize="12"/>
                                                </StackPanel>
                                            </DockPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Prescription History -->
                    <Border Style="{StaticResource Card}" Padding="0" ClipToBounds="True">
                        <StackPanel>
                            <Border Background="#FDF8F8" BorderThickness="0,0,0,1" BorderBrush="#F3F4F6" Padding="20,12">
                                <DockPanel>
                                    <TextBlock DockPanel.Dock="Left" Text="💊 " Foreground="#8A0007"/>
                                    <TextBlock x:Name="TxtRxCount" Text="Prescription History (0)" FontWeight="SemiBold" Foreground="#374151" FontSize="14"/>
                                </DockPanel>
                            </Border>
                            <TextBlock x:Name="TxtEmptyRx" Text="No prescriptions found." TextAlignment="Center" Foreground="#9CA3AF" FontSize="14" Margin="0,20,0,20" Visibility="Collapsed"/>
                            <ItemsControl x:Name="RxHistoryList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderThickness="0,0,0,1" BorderBrush="#F9FAFB" Padding="20,12">
                                            <StackPanel>
                                                <DockPanel Margin="0,0,0,4">
                                                    <Border DockPanel.Dock="Right" Background="{Binding StatusBg}" CornerRadius="12" Padding="8,2" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Status}" Foreground="{Binding StatusFg}" FontWeight="SemiBold" FontSize="12"/>
                                                    </Border>
                                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="#1F2937" FontSize="14"/>
                                                </DockPanel>
                                                <ItemsControl ItemsSource="{Binding Items}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <TextBlock Text="{Binding}" Foreground="#6B7280" FontSize="12" Margin="0,2,0,0"/>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <StackPanel x:Name="EmptySelectionPanel" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="👤" FontSize="60" Foreground="#D1D5DB" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="Select a patient to view their details" Foreground="#D1D5DB" FontSize="16"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
