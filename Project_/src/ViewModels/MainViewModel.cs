using Project_.src.Helpers;

namespace Project_.src.ViewModels
{
    public class MainViewModel : ObservableObject
    {
        // ViewModels will inherit from ObservableObject to support property change notification
        public MainViewModel()
        {
        }
    }
}
